# Filtro RC passa-altas e acoplamento {#sec-cap-034}

Troque as posições do resistor e do capacitor do @sec-cap-033 e tudo se inverte. O
mesmo par de componentes, os mesmos valores, a mesma frequência de corte — e agora o
que passa é o que antes era barrado.

A troca parece trivial, e a matemática é de fato quase idêntica. O que não é trivial é o
alcance prático do resultado. O RC passa-altas é, disparado, o circuito mais usado da
eletrônica analógica, porque ele resolve um problema que aparece em todo amplificador de
mais de um estágio: **como levar o sinal de um ponto do circuito ao seguinte sem levar
junto a tensão contínua de polarização**. A resposta é um capacitor em série, e esse
capacitor é um passa-altas — projetado ou não.

## O circuito e a função de transferência {#sec-034-circuito}

::: {#fig-034-circuito}

```{.tikz}
%%| filename: 034-circuito
%%| alt: Painel esquerdo com o esquemático do filtro RC passa-altas. Uma fonte de tensão senoidal alimenta um capacitor C em série, e do nó de saída um resistor R desce para o terra. A saída é tomada sobre o resistor, entre dois terminais abertos à direita. Painel direito com o diagrama fasorial na frequência de corte, tendo a corrente como referência horizontal. A tensão sobre o resistor, que agora é a saída, é um vetor horizontal, a tensão sobre o capacitor é um vetor de mesmo comprimento apontando para baixo, e a tensão de entrada é a soma dos dois, inclinada a menos quarenta e cinco graus. Como a saída está adiantada quarenta e cinco graus em relação à entrada, o filtro adianta a fase em vez de atrasar.

\begin{tikzpicture}[font=\footnotesize]

  % ---------------- circuito ----------------
  \begin{scope}[shift={(0,0)}]
    \draw (0,0) to[sV, l={$v_{\mathrm{in}}$}] (0,3.4);
    \draw (0,3.4) to[C, l={$C$}] (3.2,3.4);
    \draw (3.2,3.4) to[R, l_={$R$}] (3.2,0);
    \draw (0,0) -- (3.2,0);

    \draw (3.2,3.4) -- (4.8,3.4) node[ocirc]{};
    \draw (3.2,0)   -- (4.8,0)   node[ocirc]{};
    \draw[<->, >=stealth, manualgray] (5.05,0.2) -- (5.05,3.2);
    \node[anchor=west, font=\scriptsize] at (5.15,1.7) {$v_{\mathrm{out}}$};

    \draw[->, >=stealth, manualred, very thick] (0.9,4.35) -- (1.9,4.35);
    \node[manualred, anchor=south, font=\scriptsize] at (1.4,4.45) {$\mathbf{I}$};
  \end{scope}

  % ---------------- fasores em f = fc ----------------
  \begin{scope}[shift={(9.0,2.4)}]
    \draw[eixo] (-0.4,0) -- (3.6,0);
    \draw[eixo] (0,-3.4) -- (0,0.9);

    \draw[vetor, manualgreen] (0,0) -- (2.4,0);
    \node[manualgreen, anchor=south, font=\scriptsize] at (1.2,0.12)
         {$\mathbf{V}_R = \mathbf{V}_{\mathrm{out}}$};

    \draw[vetor, manualblue] (2.4,0) -- (2.4,-2.4);
    \node[manualblue, anchor=west, font=\scriptsize] at (2.5,-1.2)
         {$\mathbf{V}_C$};

    \draw[vetor, manualred] (0,0) -- (2.4,-2.4);
    \node[manualred, anchor=north east, font=\scriptsize] at (1.35,-1.25)
         {$\mathbf{V}_{\mathrm{in}}$};

    \node[manualgray, anchor=north west, font=\scriptsize] at (1.05,-0.22)
         {$+45^{\circ}$};
    \node[manualgray, anchor=south west, font=\scriptsize] at (0.0,0.95)
         {corrente como referência};
  \end{scope}

\end{tikzpicture}
```

O filtro RC passa-altas e o seu diagrama fasorial na frequência de corte. O triângulo é
o mesmo do @sec-cap-033 — o que mudou foi qual dos catetos é a saída (em verde, nas duas
figuras). Como
$\mathbf{V}_{\mathrm{out}}$ está 45° **à frente** de $\mathbf{V}_{\mathrm{in}}$, este
filtro adianta a fase.

:::

Aplicando o divisor de impedâncias, agora com a saída sobre o resistor:

$$ \mathbf{H}(j\omega)
   = \frac{R}{R + 1/j\omega C}
   = \frac{j\omega RC}{1 + j\omega RC}. $$

Os dois limites, de novo por inspeção:

- Em **$\omega \to 0$**, o numerador vai a zero e $\mathbf{H} \to 0$. O capacitor em
  série é um circuito aberto: nada de contínua chega à saída.
- Em **$\omega \to \infty$**, $\mathbf{H} \to 1$. O capacitor é um curto e o sinal
  atravessa inteiro.

Separando módulo e fase — e lembrando que multiplicar por $j$ soma 90° ao ângulo:

$$ |H| = \frac{f/f_c}{\sqrt{1+(f/f_c)^2}} = \frac{1}{\sqrt{1+(f_c/f)^2}},
   \qquad
   \phi = 90^{\circ} - \arctan\!\left(\frac{f}{f_c}\right)
        = \arctan\!\left(\frac{f_c}{f}\right), $$

com a **mesma** frequência de corte de antes:

$$ \boxed{\;f_c = \frac{1}{2\pi RC}\;} $$

A segunda forma de $|H|$ é a mais reveladora: ela é a expressão do passa-baixas com
$f$ e $f_c$ trocados de lugar. Toda a @tbl-033-valores continua valendo — basta ler a
coluna da esquerda como $f_c/f$ em vez de $f/f_c$. Em $f_c$, de novo, $|H| = 0{,}707$ e
a potência cai à metade; a diferença é que agora a fase vale $+45^{\circ}$, não
$-45^{\circ}$.

| $f/f_c$ | $|H|$ | $|H|$ em dB | $\phi$ |
|---|---|---|---|
| 0,1 | 0,0995 | −20,0 | +84,3° |
| 0,2 | 0,196 | −14,1 | +78,7° |
| 0,5 | 0,447 | −6,99 | +63,4° |
| 1 | 0,707 | −3,01 | +45,0° |
| 2 | 0,894 | −0,97 | +26,6° |
| 5 | 0,981 | −0,17 | +11,3° |
| 10 | 0,995 | −0,04 | +5,7° |

: Resposta do RC passa-altas. Compare com a @tbl-033-valores: são os mesmos números, na
ordem invertida, com a fase trocada de sinal. {#tbl-034-valores}

::: {#fig-034-resposta}

```{.tikz}
%%| filename: 034-resposta
%%| alt: Dois gráficos empilhados com o mesmo eixo horizontal logarítmico, de um centésimo da frequência de corte até cem vezes a frequência de corte. No de cima, o módulo em decibéis: a curva azul sobe da esquerda em linha reta, ganhando vinte decibéis por década, passa por menos três decibéis na frequência de corte, marcada por um ponto vermelho, e depois se achata em zero decibel. Duas retas cinzas tracejadas mostram as assíntotas, uma ascendente e uma horizontal em zero decibel, cruzando-se na frequência de corte. No de baixo, a fase em graus: a curva azul parte de mais noventa graus nas frequências muito baixas, passa por mais quarenta e cinco graus na frequência de corte e tende a zero nas frequências altas. Uma poligonal cinza tracejada aproxima a curva por três segmentos.

\begin{tikzpicture}[font=\footnotesize]

  \pgfplotsset{
    fatia/.style={
      width=15.0cm,
      xmin=0.01, xmax=100,
      xtick={0.01,0.1,1,10,100},
      xticklabels={{$0{,}01\,f_c$},{$0{,}1\,f_c$},{$f_c$},{$10\,f_c$},{$100\,f_c$}},
      grid=both, grid style={manualgray!22},
      font=\footnotesize,
    }
  }

  % ---------------- modulo ----------------
  \begin{axis}[xmode=log, fatia, height=7.6cm, at={(0cm,0cm)}, anchor=north west,
      ylabel={$|H|$ (dB)},
      ymin=-46, ymax=9,
      ytick={0,-3,-10,-20,-30,-40},
      yticklabels={{0},{$-3$},{$-10$},{$-20$},{$-30$},{$-40$}},
      xticklabels={}]

    \addplot[auxiliar, domain=-2:0, samples=2] ({10^x}, {20*x});
    \addplot[auxiliar, domain=0:2,  samples=2] ({10^x}, {0});
    \addplot[curva, domain=-2:2, samples=280]
            ({10^x}, {20*x-10*log10(1+10^(2*x))});
    \addplot[manualred, only marks, mark=*, mark size=2pt] coordinates {(1,-3.01)};

    \draw[manualred, thin] (axis cs:1.05,-4.0) -- (axis cs:2.6,-11.0);
    \node[manualred, anchor=west, font=\scriptsize] at (axis cs:2.8,-11.6)
         {$-3$ dB em $f_c$};
    \node[manualgray, anchor=north west, font=\scriptsize, align=left]
         at (axis cs:0.011,-3) {assíntota:\\$+20$ dB/década};
  \end{axis}

  % ---------------- fase ----------------
  \begin{axis}[xmode=log, fatia, height=5.8cm, at={(0cm,-8.3cm)}, anchor=north west,
      xlabel={frequência (escala logarítmica)},
      ylabel={$\phi$ (graus)},
      ymin=-14, ymax=104,
      ytick={0,45,90},
      yticklabels={{0},{$+45$},{$+90$}}]

    \addplot[auxiliar, domain=-2:-1, samples=2] ({10^x}, {90});
    \addplot[auxiliar, domain=-1:1,  samples=2] ({10^x}, {90-45*(x+1)});
    \addplot[auxiliar, domain=1:2,   samples=2] ({10^x}, {0});
    \addplot[curva, domain=-2:2, samples=280] ({10^x}, {90-atan(10^x)});
    \addplot[manualred, only marks, mark=*, mark size=2pt] coordinates {(1,45)};

    \node[manualred, anchor=west, font=\scriptsize] at (axis cs:1.6,52)
         {$+45^{\circ}$ em $f_c$};
  \end{axis}

\end{tikzpicture}
```

Resposta do RC passa-altas. É a @fig-033-resposta espelhada: a assíntota agora **sobe**
a +20 dB por década abaixo de $f_c$, e a fase vai de +90° a 0°, também ao longo de duas
décadas.

:::

A leitura das assíntotas é o espelho exato da do capítulo anterior. Para $f \ll f_c$,

$$ |H| \approx \frac{f}{f_c}, $$

isto é: **a saída cai proporcionalmente à frequência**. Uma década abaixo de $f_c$,
passa um décimo; duas décadas abaixo, um centésimo. E a fase, longe abaixo do corte,
tende a +90° — o circuito vira um derivador, como a @sec-034-tempo vai mostrar.

## Acoplamento capacitivo {#sec-034-acoplamento}

Aqui está a aplicação que justifica o capítulo.

Todo estágio amplificador precisa de **polarização**: uma tensão contínua que coloca o
transistor ou o amp-op na região onde ele funciona (Volumes 9 a 11). Um estágio pode
operar com o sinal centrado em 2,5 V; o seguinte, em 0,7 V; o terceiro, em 6 V. Ligar
um ao outro diretamente faria o nível contínuo de um destruir a polarização do outro.

O capacitor em série resolve: ele bloqueia a contínua e deixa passar a alternada. Em
regime permanente, ele se carrega com a diferença entre os dois níveis contínuos e fica
assim, enquanto o sinal atravessa por cima dessa carga.

::: {#fig-034-acoplamento}

```{.tikz}
%%| filename: 034-acoplamento
%%| alt: À esquerda, o esquemático do acoplamento capacitivo: um estágio anterior entrega um sinal centrado em dois vírgula cinco volts, um capacitor de dez microfarads em série leva esse sinal adiante, e um resistor de dez quilohms liga o nó seguinte ao terra. À direita, um gráfico do tempo com duas senoides. A azul, marcada entrada, oscila em torno da linha tracejada de dois vírgula cinco volts. A vermelha, marcada saída, tem exatamente a mesma amplitude e a mesma forma, mas oscila em torno de zero volt. A componente contínua desapareceu e o sinal permaneceu.

\begin{tikzpicture}[font=\footnotesize]

  % ---------------- circuito ----------------
  \begin{scope}[shift={(0,0.6)}]
    \draw[thick] (-0.3,1.4) rectangle (1.5,3.2);
    \node[align=center, font=\scriptsize] at (0.6,2.3) {estágio\\anterior};
    \draw (1.5,2.6) to[C, l={$10\ \mu$F}] (4.4,2.6);
    \draw (4.4,2.6) to[R, l_={$10$ k$\Omega$}] (4.4,0);
    \draw (4.4,2.6) -- (5.4,2.6) node[ocirc]{};
    \draw (-0.3,1.4) -- (-0.3,0) -- (4.4,0);
    \draw (4.4,0) -- (5.4,0) node[ocirc]{};

    \node[manualblue, anchor=west, font=\scriptsize] at (1.62,2.15)
         {$2{,}5$ V $+$ sinal};
    \node[manualred, anchor=south, font=\scriptsize] at (5.4,2.85)
         {só o sinal};
    \node[anchor=north, font=\scriptsize] at (2.6,-0.5)
         {$f_c = 1{,}59$ Hz};
  \end{scope}

  % ---------------- formas de onda ----------------
  \begin{axis}[
      at={(8.0cm,-0.4cm)}, anchor=south west,
      width=8.6cm, height=6.2cm,
      xmin=0, xmax=3, ymin=-1.3, ymax=3.5,
      axis lines=left,
      xtick=\empty,
      ytick={0,2.5}, yticklabels={{0 V},{$2{,}5$ V}},
      xlabel={tempo}, font=\footnotesize,
      y axis line style={manualgray}, x axis line style={manualgray},
    ]
    \node[manualblue, anchor=north east, font=\scriptsize]
         at (axis cs:2.98,3.45) {entrada};
    \node[manualred, anchor=north east, font=\scriptsize]
         at (axis cs:2.98,1.15) {saída};
    \addplot[auxiliar, domain=0:3, samples=2] {2.5};
    \addplot[auxiliar, domain=0:3, samples=2] {0};
    \addplot[curva,    domain=0:3, samples=240] {2.5+0.7*sin(360*x)};
    \addplot[destaque, domain=0:3, samples=240] {0.7*sin(360*x)};
  \end{axis}

\end{tikzpicture}
```

O acoplamento capacitivo em uma linha: o capacitor bloqueia os 2,5 V de polarização e
deixa passar o sinal intacto. O resistor de 10 kΩ é o que define o outro lado do
divisor — e, com ele, a frequência de corte.

:::

O ponto que costuma passar despercebido: **esse arranjo é exatamente o circuito da
@fig-034-circuito**. O capacitor de acoplamento e a resistência de entrada do estágio
seguinte formam um passa-altas, queira o projetista ou não. Se $f_c$ ficar dentro da
banda do sinal, o projeto perde graves — e a culpa não é do amplificador, é do
capacitor.

### Como escolher o capacitor de acoplamento

A regra é curta:

$$ \boxed{\;f_c \le \frac{f_{\min}}{10}
   \quad\Longrightarrow\quad
   C \ge \frac{10}{2\pi R\, f_{\min}}\;} $$

onde $R$ é a resistência vista pelo capacitor — em geral a impedância de entrada do
estágio seguinte, em paralelo com o que houver de polarização — e $f_{\min}$ é a menor
frequência que precisa passar íntegra.

O fator 10 vem da @tbl-034-valores: uma década acima de $f_c$, a atenuação é de 0,04 dB
e a fase já caiu a 5,7°. Aceitar $f_c = f_{\min}/3$ custa 0,5 dB e 18° de fase; aceitar
$f_c = f_{\min}$ custa 3 dB e 45°, e aí não é mais acoplamento, é filtro.

**Exemplo — estágio de áudio.** $R = 10$ kΩ, $f_{\min} = 20$ Hz:

$$ C \ge \frac{10}{2\pi \times 10^4 \times 20} = 7{,}96\ \mu\text{F} \;\to\; 10\ \mu\text{F}. $$

O valor comercial imediatamente acima é 10 µF (série E6), que dá
$f_c = 1{,}59$ Hz — uma década e pouco abaixo dos 20 Hz. Confortável.

**Exemplo — microfone de eletreto.** $R = 2{,}2$ kΩ, $f_{\min} = 100$ Hz:

$$ C \ge \frac{10}{2\pi \times 2200 \times 100} = 7{,}2\ \mu\text{F} \;\to\; 10\ \mu\text{F}. $$

**Exemplo — pulso digital de 1 kHz.** Aqui $f_{\min}$ não é 1 kHz: uma onda quadrada tem
componentes até bem abaixo da própria repetição quando o ciclo de trabalho não é 50 %, e
o critério passa a ser o da inclinação de topo da @sec-034-tempo, não o dos −3 dB.

::: {.callout-note}
## Maior nem sempre é melhor

A tentação é exagerar o capacitor de acoplamento: se 10 µF é bom, 470 µF é melhor. Não
é, por três motivos.

**Tempo de carga.** No momento em que o circuito liga, o capacitor precisa se carregar
com o nível contínuo, e isso leva alguns $\tau$. Com $R = 10$ kΩ e 470 µF,
$\tau = 4{,}7$ s: o amplificador leva quase meio minuto para se estabilizar, e no
caminho produz um estouro audível — o *thump* do ligar.

**Corrente de fuga.** Eletrolítico grande tem fuga da ordem de microampères. Essa
corrente atravessa o resistor de polarização e desloca o ponto de operação do estágio
seguinte. Um microampère em 1 MΩ é 1 V de erro.

**Tamanho, custo e ESR.** Acima de umas dezenas de microfarads só existe eletrolítico, e
o eletrolítico traz ESR, tolerância larga e envelhecimento (@sec-cap-022).

O capacitor de acoplamento certo é o **menor** que satisfaz a condição de $f_c$ — não o
maior que cabe na placa.
:::

### Eletrolítico no acoplamento: qual lado é o positivo

Um capacitor de acoplamento eletrolítico tem de ser montado com o terminal positivo no
lado de **maior tensão contínua média**. Se a saída de um estágio está em 2,5 V e a
entrada do seguinte em 0 V, o positivo vai para o lado dos 2,5 V.

Quando os dois lados podem inverter — ou quando a tensão contínua sobre o capacitor é
próxima de zero — a peça correta é um capacitor bipolar (não polarizado) ou de filme.
Eletrolítico polarizado com tensão reversa aquece, perde capacitância e, com tensão
suficiente, abre a válvula de alívio. O @sec-cap-002 e o @sec-cap-022 tratam disso; aqui
basta a regra.

## Onda quadrada, inclinação de topo e o derivador {#sec-034-tempo}

No domínio do tempo, o passa-altas faz o complemento exato do passa-baixas: como
$\mathbf{H}_{PA} = 1 - \mathbf{H}_{PB}$, a saída de um é a entrada menos a saída do
outro. É por isso que os degraus reaparecem inteiros na saída e os patamares é que se
perdem.

::: {#fig-034-quadrada}

```{.tikz}
%%| filename: 034-quadrada
%%| alt: Três gráficos empilhados mostrando a resposta do filtro RC passa-altas a uma onda quadrada, para três frequências. Em todos, a onda quadrada de entrada aparece em cinza tracejado, indo de menos um a mais um, e a saída aparece em azul. No painel de cima, com frequência dez vezes maior que a frequência de corte, a saída reproduz a quadrada, mas cada patamar decai um pouco ao longo do tempo, formando a inclinação de topo. No painel do meio, com frequência igual à frequência de corte, a inclinação é severa e o patamar quase chega a zero antes da próxima transição. No painel de baixo, com frequência dez vezes menor que a frequência de corte, sobraram apenas picos estreitos nas bordas, que decaem rapidamente para zero: o circuito virou um detector de bordas.

\begin{tikzpicture}[font=\footnotesize]

  \pgfplotsset{
    fatia/.style={
      width=12.8cm, height=4.0cm,
      xmin=0, xmax=3, ymin=-2.3, ymax=2.3,
      axis lines=left,
      xtick={0,1,2,3}, xticklabels={},
      ytick={-2,-1,0,1,2},
      yticklabels={{$-2$},{$-1$},{0},{1},{2}},
      font=\footnotesize,
      y axis line style={manualgray},
      x axis line style={manualgray},
    }
  }

  % ---------------- f = 10 fc ----------------
  \begin{axis}[fatia, at={(0cm,0cm)}, anchor=north west,
      declare function={
        ta=1.5915;
        sa(\t)=1.1558*exp(-\t/ta);
        ya(\t)=(mod(\t,1)<0.5)*sa(mod(\t,1))
              -(mod(\t,1)>0.5)*sa(mod(\t,1)-0.5);}]
    \addplot[auxiliar, domain=0:3, samples=1400]
            {(mod(x,1)<0.5)*1-(mod(x,1)>0.5)*1};
    \addplot[curva, domain=0:3, samples=1400] {ya(x)};
  \end{axis}
  \node[anchor=west, font=\scriptsize, align=left] at (13.1,-2.0)
       {$f = 10\,f_c$\\[2pt] inclinação de\\topo de $27$ \%};

  % ---------------- f = fc ----------------
  \begin{axis}[fatia, at={(0cm,-4.4cm)}, anchor=north west,
      declare function={
        tb=0.1592;
        sb(\t)=1.9171*exp(-\t/tb);
        yb(\t)=(mod(\t,1)<0.5)*sb(mod(\t,1))
              -(mod(\t,1)>0.5)*sb(mod(\t,1)-0.5);}]
    \addplot[auxiliar, domain=0:3, samples=1400]
            {(mod(x,1)<0.5)*1-(mod(x,1)>0.5)*1};
    \addplot[curva, domain=0:3, samples=1400] {yb(x)};
  \end{axis}
  \node[anchor=west, font=\scriptsize, align=left] at (13.1,-6.4)
       {$f = f_c$\\[2pt] o patamar\\some antes\\da próxima\\borda};

  % ---------------- f = 0,1 fc ----------------
  \begin{axis}[fatia, at={(0cm,-8.8cm)}, anchor=north west,
      xtick={0,1,2,3}, xticklabels={{0},{$T$},{$2T$},{$3T$}},
      declare function={
        tc=0.01592;
        sc(\t)=2*exp(-\t/tc);
        yc(\t)=(mod(\t,1)<0.5)*sc(mod(\t,1))
              -(mod(\t,1)>0.5)*sc(mod(\t,1)-0.5);}]
    \addplot[auxiliar, domain=0:3, samples=1400]
            {(mod(x,1)<0.5)*1-(mod(x,1)>0.5)*1};
    \addplot[curva, domain=0:3, samples=1400] {yc(x)};
  \end{axis}
  \node[anchor=west, font=\scriptsize, align=left] at (13.1,-10.8)
       {$f = 0{,}1\,f_c$\\[2pt] só as bordas\\(o derivador)};

\end{tikzpicture}
```

O passa-altas diante de uma onda quadrada. Repare que o pico de saída chega a 2 quando
a entrada vai de −1 a +1: o degrau atravessa **inteiro**, porque a tensão no capacitor
não muda instantaneamente. É o patamar que se perde.

:::

Três coisas nessa figura merecem ser ditas em palavras.

**O pico vale 2, não 1.** Quando a entrada salta de −1 para +1, a tensão sobre o
capacitor não pode mudar de imediato (@sec-cap-021), então o salto inteiro de 2 V
aparece na saída. Um passa-altas **não atenua bordas** — ele as passa integralmente e
depois deixa o resultado decair. Quem projeta acoplamento pensando em amplitude de
sinal senoidal costuma se surpreender com um pico de duas vezes na primeira borda.

**A inclinação de topo é mensurável e prevísivel.** Para $f \gg f_c$, o decaimento
durante meio período é aproximadamente linear, e a queda percentual vale

$$ \text{inclinação} \approx \frac{T}{2\tau} = \frac{\pi f_c}{f}. $$

Com $f = 10 f_c$: $\pi/10 = 31$ % pelo modelo linear, 27 % pela exponencial exata da
figura. Esse número é o critério de projeto quando o sinal é digital ou pulsado: para
uma inclinação abaixo de 1 %, é preciso $f_c \le f/300$.

**Muito abaixo de $f_c$, o circuito é um derivador.** Com $\tau$ muito menor que a
duração do pulso, a saída tende a

$$ v_{\mathrm{out}} \approx RC\,\frac{dv_{\mathrm{in}}}{dt}, $$

que é zero nos patamares e um pico em cada transição. É assim que se constrói um
detector de bordas com dois componentes — e é assim que uma borda rápida de relógio se
transforma em um pico indesejado em qualquer capacitância parasita mal colocada, assunto
recorrente do Volume 14.

## Outros usos do passa-altas {#sec-034-usos}

**Remover o nível contínuo de um sensor.** Um sensor cujo sinal útil é uma variação
pequena sobre um nível grande — um microfone, um acelerômetro em vibração, um sensor de
batimento — entrega um sinal impossível de amplificar diretamente: o ganho necessário
para a variação satura o amplificador com o nível contínuo. Um passa-altas antes do
ganho resolve, e a escolha de $f_c$ é o compromisso entre remover a contínua e preservar
a componente mais lenta que interessa.

**Filtro de rumble e de infrassom.** Em áudio, um passa-altas em 20 a 30 Hz remove o
ruído de manuseio, o sopro de vento e a ressonância mecânica sem tocar na música.

**Bloquear a contínua de um instrumento.** O acoplamento AC do osciloscópio é
literalmente um capacitor em série na entrada, com $f_c$ tipicamente entre 1 e 10 Hz.
Ele serve para ver 50 mV de ondulação sobre 12 V de contínua — e engana, porque distorce
sinais lentos e pulsos largos exatamente como a @fig-034-quadrada mostra. Volume 7.

**Compensação de ponta de prova.** A ponta 10:1 é um divisor com um passa-altas
capacitivo em paralelo com um divisor resistivo, ajustado para que os dois tenham a
mesma razão em toda a banda. Quando o ajuste está errado, a onda quadrada de calibração
sai com o topo inclinado — exatamente o efeito deste capítulo. Volume 7.

**Passa-faixa por cascata.** Um passa-altas seguido de um passa-baixas, com
$f_{c,\text{PA}} \ll f_{c,\text{PB}}$, deixa passar só a faixa entre os dois. É o filtro
passa-faixa mais barato que existe, e o @sec-cap-036 mostra por que ele não pode ser
montado ingenuamente em cascata direta.

## 🧪 Laboratório {#sec-034-lab}

**Levantar a curva do passa-altas, medir a inclinação de topo, e ver o acoplamento
capacitivo apagar uma tensão contínua sem tocar no sinal.**

### Material

::: {.lab-material}
- 1 multímetro digital com escala CA e escala CC
- 1 gerador de sinal: celular ou computador com aplicativo gerador de tom, e cabo com
  plugue P2 e pontas descascadas
- 1 resistor de 1 kΩ e 1 de 10 kΩ, 1/4 W, **medidos** e anotados
- 2 resistores de 10 kΩ para o divisor da Parte 4
- 1 capacitor de 100 nF, poliéster ou cerâmico
- 1 capacitor de 1 µF, poliéster
- 1 capacitor eletrolítico de 10 µF, 25 V (Parte 4)
- 1 fonte de bancada ou pilha de 9 V com divisor, para o nível contínuo da Parte 4
- Protoboard, jumpers, papel milimetrado
:::

### Parte 1 — A curva espelhada

1. Monte o passa-altas: capacitor de 100 nF entre a saída do gerador e o nó de saída,
   resistor de 1 kΩ do nó de saída ao terra. É o circuito do @sec-cap-033 com as duas
   peças trocadas de lugar — literalmente as mesmas peças.
2. Calcule $f_c = 1/(2\pi RC) = 1{,}59$ kHz. É a mesma de antes.
3. Meça a razão $v_{\mathrm{out}}/v_{\mathrm{in}}$ em 100, 500, 800 Hz, 1, 1,6, 3, 5, 10
   e 16 kHz.

| $f$ | $v_{\mathrm{in}}$ | $v_{\mathrm{out}}$ | razão | previsto |
|---|---|---|---|---|
| 100 Hz | | | | 0,063 |
| 500 Hz | | | | 0,300 |
| 1,0 kHz | | | | 0,532 |
| 1,6 kHz | | | | 0,709 |
| 3,0 kHz | | | | 0,883 |
| 5,0 kHz | | | | 0,953 |
| 10 kHz | | | | 0,988 |
| 16 kHz | | | | 0,995 |

: Tabela da Parte 1, para $R = 1$ kΩ e $C = 100$ nF. A coluna prevista usa
$1/\sqrt{1+(1591/f)^2}$. {#tbl-034-lab}

4. Ponha esta tabela lado a lado com a @tbl-033-lab do capítulo anterior e some, linha a
   linha, os quadrados das duas razões. A soma deve dar 1 em toda linha:

$$ |H_{PB}|^2 + |H_{PA}|^2 = 1. $$

Essa identidade não é coincidência — é a razão de $f_c$ ser chamada de frequência de
meia potência. Em $f_c$, cada filtro entrega metade da potência, e as duas metades
fecham o total.

### Parte 2 — Achar $f_c$ subindo em vez de descendo

1. Anote $v_{\mathrm{out}}$ em 16 kHz, bem acima do corte. Chame de $V_\infty$.
2. Calcule $0{,}707\,V_\infty$ e varra a frequência **para baixo** até encontrar esse
   valor. Compare com 1,59 kHz.
3. Troque $C$ por 1 µF: $f_c$ cai para 159 Hz. Confirme com três pontos.

### Parte 3 — A inclinação de topo

Esta parte é a mais instrutiva do roteiro e dispensa osciloscópio se você tiver acesso a
um aplicativo de osciloscópio por placa de som; se não tiver nenhum dos dois, use a
variante simulada.

1. Ponha o gerador em onda quadrada.
2. Com $R = 10$ kΩ e $C = 100$ nF ($f_c = 159$ Hz), observe a saída em 1,6 kHz.
   Deve aparecer a quadrada com o topo caindo — a inclinação prevista é
   $\pi f_c/f = \pi \times 159/1600 = 31$ %.
3. Meça a queda: amplitude no começo do patamar contra amplitude no fim. Compare.
4. Baixe para 160 Hz. A inclinação vira quase um decaimento completo.
5. Baixe para 16 Hz. Sobram picos: o derivador.

### Parte 4 — O acoplamento apagando a contínua

1. Monte um divisor com dois resistores de 10 kΩ entre 5 V e o terra, criando um nó em
   2,5 V. Injete o sinal do gerador nesse nó através de um capacitor de 1 µF: esse é o
   "estágio anterior" com o sinal montado sobre 2,5 V de polarização.
2. Meça esse nó com o multímetro em **CC**: deve dar 2,5 V. Meça em **CA**: deve dar a
   amplitude eficaz do sinal.
3. Ligue o eletrolítico de 10 µF em série a partir desse nó — **positivo do lado dos
   2,5 V** — e um resistor de 10 kΩ do outro terminal para o terra.
4. Meça o novo nó em CC: deve dar praticamente **0 V**. Meça em CA: deve dar
   praticamente a mesma tensão de antes.

Esses dois pares de medidas são a @fig-034-acoplamento na bancada. O nível contínuo
sumiu e o sinal ficou.

5. Ponha o gerador em 1 kHz, depois em 100 Hz, depois em 10 Hz, medindo a tensão CA nos
   dois lados do capacitor. Em 10 Hz a saída já começa a cair — $f_c$ deste acoplamento
   é 1,59 Hz e a atenuação em 10 Hz vale 1,25 %.
6. Troque o eletrolítico por 100 nF ($f_c = 159$ Hz) e repita. Agora 100 Hz já perde
   quase 4 dB. É o acoplamento mal dimensionado, medido.

**Variante simulada (plano B).** No ngspice, `.ac dec 30 1 1e5` sobre o circuito da
Parte 1 desenha a @fig-034-resposta inteira; `.tran` com fonte `PULSE` reproduz a
@fig-034-quadrada. No Falstad, o efeito da Parte 4 é imediato: basta arrastar a fonte
para "AC + DC offset" e observar os dois lados do capacitor.

## 🔥 Onde Queima {#sec-034-queima}

**Esquecer que o capacitor de acoplamento é um filtro.** Ele e a resistência do estágio
seguinte formam um passa-altas com $f_c = 1/(2\pi RC)$. Amplificador sem graves quase
sempre é acoplamento subdimensionado, não falha do amplificador.

**Usar $R$ errado na conta de $f_c$.** O $R$ que conta é o **visto pelo capacitor**: a
impedância de entrada do estágio seguinte em paralelo com os resistores de polarização,
não o resistor que está desenhado mais perto.

**Inverter o eletrolítico de acoplamento.** O positivo vai no lado de maior tensão média.
Invertido, ele esquenta, perde capacitância e pode abrir a válvula. Quando os dois lados
podem inverter, use bipolar ou filme.

**Exagerar no capacitor de acoplamento.** Capacitância grande demais traz *thump* no
ligar, tempo de acomodação de segundos e corrente de fuga que desloca a polarização do
estágio seguinte.

**Projetar acoplamento por amplitude senoidal e receber uma borda de duas vezes.** Um
degrau atravessa o passa-altas **inteiro**. Se a entrada salta 5 V, a saída salta 5 V —
sobre o que houver de polarização do outro lado.

**Ignorar a inclinação de topo em sinal pulsado.** O critério dos −3 dB não serve para
pulsos. Para menos de 1 % de inclinação, é preciso $f_c \le f/300$.

**Confiar no acoplamento AC do osciloscópio para medir pulsos largos.** Ele é este
circuito, com $f_c$ de alguns hertz, e distorce o topo do pulso exatamente como a
@fig-034-quadrada. Para medir nível contínuo com ondulação, meça as duas coisas
separadamente.

**Achar que o passa-altas atenua a fase da mesma forma que o passa-baixas.** Ele
**adianta**: +90° longe abaixo do corte, +45° em $f_c$. Em malha de realimentação, o
sinal do desvio de fase muda tudo.

**Deixar um nó sem caminho de contínua para o terra.** Depois do capacitor de
acoplamento, o nó precisa de um resistor até uma referência. Sem ele, o nó flutua, a
fuga do capacitor o carrega até um valor imprevisível e o estágio seguinte satura.

**Somar capacitores de acoplamento em cascata sem contar.** Três estágios acoplados, cada
um com $f_c = 20$ Hz, atenuam 9 dB em 20 Hz, não 3 dB. Cada acoplamento é um polo; três
polos na mesma frequência somam. Assunto do @sec-cap-036.

**Usar cerâmico de alta constante dielétrica em acoplamento de áudio.** Os tipos X7R e
Y5V perdem capacitância com a tensão aplicada e são microfônicos: batidas na placa viram
sinal. Em acoplamento, filme ou eletrolítico.

## O que fica deste capítulo {#sec-034-resumo}

Trocando o resistor e o capacitor de posição, o divisor vira um passa-altas:

$$ \mathbf{H}(j\omega) = \frac{j\omega RC}{1 + j\omega RC},
   \qquad
   |H| = \frac{1}{\sqrt{1+(f_c/f)^2}},
   \qquad
   \phi = \arctan(f_c/f), $$

com a **mesma** $f_c = 1/(2\pi RC)$. Em $f_c$: $|H| = 0{,}707$ e $\phi = +45^{\circ}$.
As assíntotas são +20 dB por década abaixo do corte e 0 dB acima; a fase vai de +90° a 0°
ao longo de duas décadas.

Os dois filtros são complementares em potência: $|H_{PB}|^2 + |H_{PA}|^2 = 1$ em toda
frequência.

A aplicação central é o **acoplamento capacitivo**: um capacitor em série bloqueia a
contínua de polarização e deixa passar o sinal. O critério de projeto é
$C \ge 10/(2\pi R f_{\min})$, com $R$ igual à resistência **vista pelo capacitor**.
Exagerar não é seguro: capacitância grande demais traz *thump*, tempo de acomodação e
fuga.

Em sinal pulsado, o parâmetro que manda não é a atenuação, é a **inclinação de topo**,
$\approx \pi f_c / f$. E um degrau atravessa o passa-altas inteiro, produzindo um pico
de duas vezes a amplitude na primeira transição. Muito abaixo de $f_c$, o circuito é um
derivador e sobra só a borda.

Os dois capítulos escreveram a mesma história duas vezes, com fórmulas quase iguais e
assíntotas espelhadas. Não é acaso: existe uma linguagem em que os dois filtros são o
mesmo objeto com um sinal trocado, e em que traçar a resposta de qualquer circuito passa
a ser somar retas em vez de calcular módulos. Essa linguagem é o diagrama de Bode, e é o
@sec-cap-035.
